package rose

type IndexFsHandler struct {

}

type Index struct {

}

func NewIndex(collName string, fieldName string) {
/*	idxRep := fmt.Sprintf("%s%s%s", collName, delim, fieldName)
	idxLoc := roseIndexLocation()

	stat, err := os.Stat(idxLoc)

	if err != nil {
		return newError()
	}*/
}
